<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <li style="list-style:none"><%= check_box_tag(:completed, true, task.completed)%> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to '+ Add new restaurant', new_task_path  %>